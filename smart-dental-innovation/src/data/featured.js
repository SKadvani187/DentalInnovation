// Featured showcase is DB-only — fetched via settings API (site_settings key 'featured',
// merged in SettingsContext). Empty so the import resolves; DB value always overrides.
export const featured = [];
